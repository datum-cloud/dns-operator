---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: dnszonetsigkeys.dns.networking.miloapis.com
spec:
  group: dns.networking.miloapis.com
  names:
    kind: DNSZoneTSIGKey
    listKind: DNSZoneTSIGKeyList
    plural: dnszonetsigkeys
    shortNames:
    - tsig
    singular: dnszonetsigkey
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Accepted")].status
      name: Accepted
      type: string
    - jsonPath: .status.conditions[?(@.type=="Programmed")].status
      name: Programmed
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DNSZoneTSIGKey is the Schema for the DNSZone TSIG keys API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DNSZoneTSIGKey
            properties:
              algorithm:
                default: hmac-md5
                description: Algorithm is the TSIG algorithm used for the key.
                enum:
                - hmac-md5
                - hmac-sha1
                - hmac-sha224
                - hmac-sha256
                - hmac-sha384
                - hmac-sha512
                type: string
              dnsZoneRef:
                description: |-
                  DNSZoneRef references the DNSZone (same namespace) this TSIG key is associated with.
                  The controller derives provider configuration from the referenced zone.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: dnsZoneRef.name must be set
                  rule: self.name != ''
              keyName:
                description: |-
                  KeyName is the provider-visible name for this TSIG key (the "wire name").
                  Allow DNS name syntax with optional trailing dot. TSIG key names are DNS names (case-insensitive).
                maxLength: 253
                minLength: 1
                pattern: ^([A-Za-z0-9_](?:[-A-Za-z0-9_]{0,61}[A-Za-z0-9_])?)(?:\.([A-Za-z0-9_](?:[-A-Za-z0-9_]{0,61}[A-Za-z0-9_])?))*\.?$
                type: string
                x-kubernetes-validations:
                - message: keyName is immutable and cannot be changed after creation
                  rule: oldSelf == '' || self == oldSelf
              secretRef:
                description: |-
                  SecretRef references an existing Secret containing TSIG material (BYO secret).
                  When set, the controller reads and validates the Secret, but must not mutate it.
                  When omitted, the controller generates and manages a Secret named deterministically.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: secretRef.name must be set
                  rule: self == null || self.name != ''
            required:
            - dnsZoneRef
            - keyName
            type: object
          status:
            description: status defines the observed state of DNSZoneTSIGKey
            properties:
              conditions:
                description: Conditions tracks state such as Accepted and Programmed
                  readiness.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              secretName:
                description: SecretName is the name of the Secret used for this TSIG
                  key (generated or referenced).
                type: string
              tsigKeyName:
                description: TSIGKeyName is the provider-visible name for this TSIG
                  key.
                type: string
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.dnsZoneRef.name
    served: true
    storage: true
    subresources:
      status: {}
