# SPDX-License-Identifier: AGPL-3.0-only

# ActivityPolicy for DNSRecordSet resources.
# Defines how DNSRecordSet API operations and controller events appear in activity timelines.
#
# Audit rules handle CRUD operations captured by the Kubernetes API server audit log.
# Event rules handle async controller events for programming outcomes.
#
# Design principles:
# - Use FQDNs (www.example.com) as display text, not resource names
# - Show record values (IPs, CNAME targets, MX hosts) where available
# - Action-oriented language ("added", "removed", "is now resolving to")
# - Type-specific phrasing for common record types
# - Graceful fallback using requestObject.spec when responseObject.spec is unavailable
#   (some audit levels omit spec from responseObject)
# - Last-resort fallback that requires no spec access at all
#
# Controller-provided annotations on resource (for audit rules):
#   dns.networking.miloapis.com/display-name: www.example.com
#   dns.networking.miloapis.com/display-value: 192.0.2.10
#
# Controller-provided annotations on events:
#   dns.networking.miloapis.com/display-name: www.example.com
#   dns.networking.miloapis.com/cname-target: api.internal.example.com
#   dns.networking.miloapis.com/mx-hosts: 10 mail.example.com, 20 mail2.example.com
apiVersion: activity.miloapis.com/v1alpha1
kind: ActivityPolicy
metadata:
  name: dns.networking.miloapis.com-dnsrecordset
spec:
  resource:
    apiGroup: dns.networking.miloapis.com
    kind: DNSRecordSet

  # Audit log rules for CRUD operations.
  # These use display-name and display-value annotations set by the controller.
  # Rules are ordered from most specific to fallback.
  #
  # IMPORTANT: Match expressions use requestObject.spec (not responseObject.spec)
  # because responseObject may lack spec at lower audit levels (e.g. Metadata).
  # Summary templates use responseObject.metadata.annotations which are always present.
  auditRules:
    # ----- CREATE RULES (type-specific with display annotations) -----
    # A/AAAA records with display annotations: "added www.example.com pointing to 192.0.2.10"
    - name: create-a-aaaa
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType in ['A', 'AAAA'] && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} added {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} pointing to {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # CNAME records with display annotations: "added api.example.com as an alias for api.internal.example.com"
    - name: create-cname
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType == 'CNAME' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} added {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} as an alias for {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # ALIAS records with display annotations: "added example.com as an alias for lb.example.com"
    - name: create-alias
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType == 'ALIAS' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} added {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} as an alias for {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # MX records with display annotations: "configured mail for example.com using 10 mail.example.com"
    - name: create-mx
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType == 'MX' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} configured mail for {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} using {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # TXT records with display annotations: "added TXT record for example.com"
    - name: create-txt
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType == 'TXT' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} added a TXT record for {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }}"

    # NS records with display annotations: "added nameservers for sub.example.com"
    - name: create-ns
      match: "verb == 'create' && has(requestObject.spec) && requestObject.spec.recordType == 'NS' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} added nameservers for {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }}"

    # Generic create with display annotations: "added SRV record for _sip._tcp.example.com"
    - name: create-other-annotated
      match: "verb == 'create' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations && has(requestObject.spec)"
      summary: "{{ actor }} added a {{ requestObject.spec.recordType }} record for {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }}"

    # Create with requestObject.spec but no display annotations: "added an A record"
    # Uses requestObject.spec which is available at Request audit level and above.
    - name: create-from-request
      match: "verb == 'create' && has(requestObject.spec)"
      summary: "{{ actor }} added {{ link('a ' + requestObject.spec.recordType + ' record', objectRef) }}"

    # Create last resort (no spec available at all): "added a DNS record"
    - name: create-fallback
      match: "verb == 'create'"
      summary: "{{ actor }} added {{ link('a DNS record', objectRef) }}"

    # ----- DELETE RULES -----
    # Delete with display annotations available: "removed www.example.com"
    - name: delete-annotated
      match: "verb == 'delete' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} removed {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }}"

    # Delete with responseObject.spec available: "removed an A record"
    - name: delete-from-response
      match: "verb == 'delete' && has(responseObject.spec)"
      summary: "{{ actor }} removed {{ link('a ' + responseObject.spec.recordType + ' record', objectRef) }}"

    # Delete last resort: "removed a DNS record"
    - name: delete-fallback
      match: "verb == 'delete'"
      summary: "{{ actor }} removed {{ link('a DNS record', objectRef) }}"

    # ----- UPDATE RULES (type-specific with display annotations) -----
    # A/AAAA records with display annotations: "updated www.example.com to point to 192.0.2.20"
    - name: update-a-aaaa
      match: "verb in ['update', 'patch'] && !has(objectRef.subresource) && has(requestObject.spec) && requestObject.spec.recordType in ['A', 'AAAA'] && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} updated {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} to point to {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # CNAME records with display annotations: "updated api.example.com to alias api.v2.example.com"
    - name: update-cname
      match: "verb in ['update', 'patch'] && !has(objectRef.subresource) && has(requestObject.spec) && requestObject.spec.recordType == 'CNAME' && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations"
      summary: "{{ actor }} updated {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }} to alias {{ responseObject.metadata.annotations['dns.networking.miloapis.com/display-value'] }}"

    # Generic update with display annotations
    - name: update-other-annotated
      match: "verb in ['update', 'patch'] && !has(objectRef.subresource) && has(responseObject.metadata.annotations) && 'dns.networking.miloapis.com/display-name' in responseObject.metadata.annotations && has(requestObject.spec)"
      summary: "{{ actor }} updated {{ requestObject.spec.recordType }} record {{ link(responseObject.metadata.annotations['dns.networking.miloapis.com/display-name'], responseObject) }}"

    # Update with requestObject.spec but no display annotations: "updated an A record"
    - name: update-from-request
      match: "verb in ['update', 'patch'] && !has(objectRef.subresource) && has(requestObject.spec)"
      summary: "{{ actor }} updated {{ link('a ' + requestObject.spec.recordType + ' record', objectRef) }}"

    # Update last resort: "updated a DNS record"
    - name: update-fallback
      match: "verb in ['update', 'patch'] && !has(objectRef.subresource)"
      summary: "{{ actor }} updated {{ link('a DNS record', objectRef) }}"

    # Status subresource updates are system-initiated and typically not shown to users
    # - name: update-status
    #   match: "verb in ['update', 'patch'] && objectRef.subresource == 'status'"
    #   summary: "System updated status of {{ link(objectRef.name, responseObject) }}"

  # Event rules for controller-emitted Kubernetes events.
  # These capture async outcomes that audit logs cannot represent.
  # Events always have annotations set by the controller, so no fallback needed.
  #
  # Available annotations on all events:
  #   dns.networking.miloapis.com/event-type: dns.recordset.programmed
  #   dns.networking.miloapis.com/domain-name: example.com
  #   dns.networking.miloapis.com/record-type: A
  #   dns.networking.miloapis.com/record-names: www
  #   dns.networking.miloapis.com/record-count: 1
  #   dns.networking.miloapis.com/display-name: www.example.com
  #
  # Type-specific annotations:
  #   dns.networking.miloapis.com/ip-addresses: 192.0.2.10 (A/AAAA only)
  #   dns.networking.miloapis.com/cname-target: target.example.com (CNAME only)
  #   dns.networking.miloapis.com/mx-hosts: 10 mail.example.com (MX only)
  eventRules:
    # ----- PROGRAMMED EVENTS (type-specific, conversational tone) -----
    # A/AAAA records: "Your record is live! www.example.com now resolves to 192.0.2.10"
    - name: programmed-a-aaaa
      match: "reason == 'RecordSetProgrammed' && metadata.annotations['dns.networking.miloapis.com/record-type'] in ['A', 'AAAA']"
      summary: "Your record is live! {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} now resolves to {{ metadata.annotations['dns.networking.miloapis.com/ip-addresses'] }}"

    # CNAME records: "Your record is live! api.example.com is now an alias for api.internal.example.com"
    - name: programmed-cname
      match: "reason == 'RecordSetProgrammed' && metadata.annotations['dns.networking.miloapis.com/record-type'] == 'CNAME'"
      summary: "Your record is live! {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} is now an alias for {{ metadata.annotations['dns.networking.miloapis.com/cname-target'] }}"

    # MX records: "Mail is ready! example.com now routes to 10 mail.example.com"
    - name: programmed-mx
      match: "reason == 'RecordSetProgrammed' && metadata.annotations['dns.networking.miloapis.com/record-type'] == 'MX'"
      summary: "Mail is ready! {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} now routes to {{ metadata.annotations['dns.networking.miloapis.com/mx-hosts'] }}"

    # TXT records: "Your TXT record for example.com is now live"
    - name: programmed-txt
      match: "reason == 'RecordSetProgrammed' && metadata.annotations['dns.networking.miloapis.com/record-type'] == 'TXT'"
      summary: "Your TXT record for {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} is now live"

    # Generic programmed fallback: "Your NS record for sub.example.com is now live"
    - name: programmed-other
      match: "reason == 'RecordSetProgrammed'"
      summary: "Your {{ metadata.annotations['dns.networking.miloapis.com/record-type'] }} record for {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} is now live"

    # ----- PROGRAMMING FAILED EVENTS -----
    # "We hit a snag configuring www.example.com — Provider API error"
    - name: programming-failed
      match: "reason == 'RecordSetProgrammingFailed'"
      summary: "We hit a snag configuring {{ link(metadata.annotations['dns.networking.miloapis.com/display-name'], regarding) }} — {{ metadata.annotations['dns.networking.miloapis.com/failure-reason'] }}"
