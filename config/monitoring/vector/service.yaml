apiVersion: v1
kind: Service
metadata:
  name: vector
  labels:
    app.kubernetes.io/name: vector
spec:
  selector:
    app.kubernetes.io/name: vector
  ports:
    - name: vector
      port: 9000
      targetPort: vector
      protocol: TCP
